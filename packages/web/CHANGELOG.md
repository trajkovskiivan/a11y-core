# @compa11y/web

## 0.2.1

### Minor Changes

- Packages update

## 0.2.0

### Patch Changes

- Add `funding` and `engines` metadata fields to package.json
- Add missing sub-path exports for Toast, VisuallyHidden, SkipLink, Alert, Link, Text, Popover, Accordion, Table, FormField, Pagination, and Breadcrumbs
- Move to full compa11y nomenclature

## 0.1.11

### Minor Changes

- Add `<compa11y-breadcrumbs>` web component with collapse state

## 0.1.10

### Minor Changes

- Add `<compa11y-pagination>` web component with `page-change` and `page-size-change` events

## 0.1.9

### Minor Changes

- Add `<compa11y-heading>` and `<compa11y-text>` web components
- Add `<compa11y-form-field>` web component
- Add `<compa11y-popover>` web component with placement support
- Add `<compa11y-accordion>` web component
- Add `<compa11y-table>` web component with sort and select support

## 0.1.8

### Minor Changes

- Add `<compa11y-link>` web component
- Fix toast event names for clarity

## 0.1.7

### Minor Changes

- Add `<compa11y-visually-hidden>` web component
- Add `<compa11y-skip-link>` web component
- Add `<compa11y-alert>` web component

## 0.1.6

### Minor Changes

- Add `<compa11y-switch>` web component
- Update `<compa11y-checkbox>` and `<compa11y-radio-group>` web components

## 0.1.5

### Minor Changes

- Add `<compa11y-listbox>` web component
- Add `<compa11y-select>` web component with type-ahead support

## 0.1.4

### Minor Changes

- Add `<compa11y-input>` web component
- Add `<compa11y-textarea>` web component
- Add `<compa11y-button>` web component

## 0.1.3

### Minor Changes

- Add `<compa11y-toast>` web component with live region announcements

## 0.1.2

### Minor Changes

- Add `<compa11y-combobox>` web component
- Add `<compa11y-tabs>` web component

## 0.1.1

### Minor Changes

- Add `<compa11y-dialog>` web component with focus trap
- Add `<compa11y-menu>` web component

## 0.1.0

### Major Changes

- Initial release of `@compa11y/web` — accessible Web Components, CDN ready
